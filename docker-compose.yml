version: '3.8'

services:
  hypeclip:
    build: .
    container_name: hypeclip-app
    ports:
      - "5002:5002"
    volumes:
      - ./output:/app/output
    environment:
      - FLASK_ENV=production
      - SECRET_KEY=change-me-in-production
    restart: unless-stopped
